# Lab 1 - Tristan Ko

## What I learned 

I learned that when you use equalities, you should use "===" rather than "==". 
I also learned that the reason we are using JavaScript is because it runs oni the browser, runs on the server (node.js), and is the foundation for React. 

What I ran in the terminal:

```javascript
0.1 + 0.2
typeof null
[] == false 
const x = 5;
console.log(x)
```

Output: 
```
0.30000000000000004
'object'
true
undefined
5
```
